# App nativa de macOS (CASAMBI.app). Instalar con:
#   .venv/bin/pip install -r requirements.txt
# Arrancar en desarrollo:  .venv/bin/python desktop.py
# Empaquetar:              .venv/bin/pyinstaller CASAMBI.spec --noconfirm

requests>=2.31.0
openpyxl>=3.1.2
python-dotenv>=1.0.0
Pillow>=10.0.0
PyMuPDF>=1.24.0
Flask>=3.0.0
websocket-client>=1.6.0

# Ventana nativa (WKWebView) y empaquetado
pywebview>=5.0
pyinstaller>=6.0
