# Solo el servidor web. Ninguna de estas se empaqueta en el .app de macOS.

gunicorn==23.0.0        # servidor WSGI; el de Werkzeug es de desarrollo
cryptography==50.0.1    # Fernet, para el fichero de credenciales cifrado
PyJWT[crypto]==2.14.0   # verificación del JWT de Cloudflare Access
